// 2. arrow function
const getFiftyFive = () => 55; //simple parameter (nai) wala arrow function
const addSixtyFive = num => num + 65;// 1 ta parameter wala arrow function
const isEven = x => x % 2 == 0;
// multiple parameter
const addThree = (x, y, z) => x + y + z;
// multiline arrow function
const doMath = (num1, num2) => {
    const sum = num1 + num2;
    return sum;
}